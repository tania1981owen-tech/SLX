name: Automated Windows Solution Build

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Clone repository
      uses: actions/checkout@v4

    - name: Setup MSBuild Configuration
      uses: microsoft/setup-msbuild@v2

    # Устанавливаем библиотеки через vcpkg
    - name: Install Dependencies via vcpkg
      run: |
        vcpkg install yaml-cpp:x86-windows
        vcpkg install cryptopp:x86-windows
        vcpkg integrate install

    # Восстанавливаем NuGet пакеты
    - name: Restore NuGet Packages
      run: nuget restore alx.sln

    # [ИСПРАВЛЕНО] Добавлен флаг /p:AdditionalOptions="/FIyaml-cpp/yaml.h /FIcryptopp/sha.h"
    # Это принудительно заставляет компилятор вшить нужные заголовочные файлы в каждый .cpp файл проекта перед компиляцией.
    # Так мы полностью чиним ошибки "is not a class or namespace name" без изменения самих исходников.
    # Также добавлен макрос CRYPTOPP_DISABLE_ASM для совместимости со сборкой x86.
    - name: Compile Solution
      run: |
        msbuild alx.sln /t:Rebuild /p:Configuration=Release /p:Platform="Win32" /p:PlatformToolset=v143 /p:WindowsTargetPlatformVersion=10.0 /p:UseVcpkgIntegration=true /p:VcpkgEnableManifest=false /p:VcpkgTriplet=x86-windows /p:AdditionalIncludeDirectories="C:\vcpkg\installed\x86-windows\include" /p:AdditionalLibraryDirectories="C:\vcpkg\installed\x86-windows\lib" /p:AdditionalOptions="/FIyaml-cpp/yaml.h /FIcryptopp/sha.h /FIcryptopp/cryptlib.h /DCRYPTOPP_DISABLE_ASM"

    # Сохраняем готовую DLL-ку
    - name: Save Compiled Files
      uses: actions/upload-artifact@v4
      with:
        name: Finished-Program
        path: |
          Release/*.dll
          Release/*.exe
          **/Release/*.dll
          **/Release/*.exe
